# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=false
org.gradle.parallel=true

# Project
group=com.theasshats.pcmcterritory
mod_version=0.1.0

# Mod properties
mod_id=pcmc_territory
mod_name=PCMC Territory
mod_license=MIT
mod_authors=theasshats
mod_description=Territory layer for Project Commonwealth governance: given a (level, chunk), resolves the political entity that governs it. Reads MineColonies colonies and Open Parties and Claims chunk claims; owns no claims of its own.

# Minecraft / NeoForge
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.21.2)
neo_version=21.1.233
neo_version_range=[21.1.0,)
loader_version_range=[4,)

# Java toolchain
java_version=21

# Soft dependencies (compile-time only; mod degrades to no-op if either is absent at runtime)
minecolonies_curse_project=245506
minecolonies_curse_file=8186694
minecolonies_version_range=[1.1.1327,)

opac_mod_id=openpartiesandclaims
opac_version=0.26.2
opac_version_range=[0.26.0,)
